const margin = {
	top: 40,
	bottom: 40,
	left: 40,
	right: 40,
};

const width = 400;
const height = 300;
const innerWidth: number = width - margin.left - margin.right;
const innerHeight: number = height - margin.top - margin.bottom;

export { margin, width, height, innerHeight, innerWidth };
